import { styledPropsBackgroundColor } from "assets/style/propsFunctions";

const { default: styled } = require("styled-components");

export const HeaderStyled = styled.header`
    width: 100vw;
    background-color: ${styledPropsBackgroundColor};
`
